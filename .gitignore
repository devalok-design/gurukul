node_modules/

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
*.log
.DS_Store

# env
.env
.env.*

# typescript
*.tsbuildinfo
package-lock.json

# Build output (Astro-era leftovers + Next). Untracked but not ignored before,
# which let a `git add -A` sweep them into a commit once.
dist/
.astro/
